# GraphQL

## Apollo Server

-   [x] installasi
-   [x] Schema (TypeDefs)
    ```js
    gql`
        # ini komentar
    `;
    ```
-   [x] Resolvers
    -   [x] Query
    -   [x] Mutation
        -   [x] dengan argument
-   [x] `const { makeExecutableSchema } = require("@graphql-tools/schema");`
